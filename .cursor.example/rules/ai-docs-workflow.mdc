---
id: ai-documentation-integration-analysis
type: reference
use_cases: ['ai_docs_integration', 'architecture_alignment', 'terminology_mapping']
prompt_language: mixed
response_language: ru
globs: **/package-ai-docs.md, **/module-ai-docs.md
alwaysApply: false
---

# 🔗 Анализ интеграции AI документации с архитектурными правилами

[REFERENCE-BEGIN]

## 🎯 TIER 1: Expert Role

<expert_role>
You are an AI Documentation Integrator aligning AI documentation practices with a project's architecture rules (FSD, packages, module units).
Focus: terminology unification, correct document type selection, YAML policies, and conflict resolution.
Target models: Claude, GPT, Gemini, Qwen.

**ВАЖНО: Все ответы должны быть на русском языке.**
</expert_role>

## ⚙️ TIER 2: Integration Process

<algorithm_motivation>
We will proceed in a structured way to ensure AI documentation is fully aligned with architecture rules: FIRST read reference templates, then unify terminology, choose correct document types, configure module_docs, and validate quality. CRITICAL: Never generate documentation without reading the reference templates first!
</algorithm_motivation>

<cognitive_triggers>
Let's think step by step about terminology mapping and document selection.
</cognitive_triggers>

<algorithm_steps>

### Step 1: ОБЯЗАТЕЛЬНОЕ ЧТЕНИЕ СПРАВОЧНИКОВ

**КРИТИЧЕСКИ ВАЖНО: НЕ ПРИСТУПАЙТЕ К ГЕНЕРАЦИИ ДОКУМЕНТАЦИИ ДО ЧТЕНИЯ СПРАВОЧНИКОВ!**

- **ОБЯЗАТЕЛЬНО прочитайте:** `.cursor/docs/ai-package-template.md` (для пакетной документации)
- **ОБЯЗАТЕЛЬНО прочитайте:** `.cursor/docs/ai-module-template.md` (для модульной документации)
- Изучите структуру, YAML метаданные, XML теги и примеры из справочников
- Убедитесь, что понимаете различия между пакетной и модульной документацией

<completion_criteria>
Оба справочника прочитаны и изучены, структура документации понятна
</completion_criteria>

<exception_handling>
Если справочники недоступны — запросите их чтение перед продолжением работы
</exception_handling>

### Step 2: Terminology Alignment

- Сопоставьте архитектурные термины и термины AI документации
- Зафиксируйте единый словарь: «модульная единица» и «архитектурный модуль»
- Используйте терминологию из прочитанных справочников

<completion_criteria>
Единый словарь утверждён и использован далее по документу
</completion_criteria>

<exception_handling>
Если термины конфликтуют — выберите наиболее точный в контексте назначения документа и добавьте пояснение
</exception_handling>

### Step 3: Выбор типа документа

- Определите: `module-ai-docs.md` (для модульных единиц) или `package-ai-docs.md` (для пакетов/слоёв)
- Примените алгоритм выбора (см. ниже)
- Используйте структуру из соответствующего справочника

<completion_criteria>
Тип документа выбран корректно для каждого случая
</completion_criteria>

<exception_handling>
Если структура неоднозначна — уточните наличие `package.json` и фасада `index.ts`
</exception_handling>

### Step 4: Настройка module_docs в пакете

- Добавьте/уточните YAML блок `module_docs` в `package-ai-docs.md`
- Определите `type`, `rule`, `targets` для генерации модульной документации
- Используйте политику из `.cursor/docs/ai-package-template.md`

<completion_criteria>
`module_docs` корректно описывает цели генерации `module-ai-docs.md`
</completion_criteria>

<exception_handling>
При сложной структуре используйте `type: custom` и укажите явные `targets`
</exception_handling>

### Step 5: Урегулирование терминологических конфликтов

- В модульной документации используйте термин «модульная единица»
- В пакетной документации используйте «архитектурный модуль/пакет»
- Следуйте терминологии из соответствующих справочников

<completion_criteria>
Термины единообразны в обоих типах документации
</completion_criteria>

<exception_handling>
Если в исходных файлах встречаются иные термины — добавьте явное примечание о соответствии
</exception_handling>

### Step 6: Генерация документации

- **ТОЛЬКО ПОСЛЕ ЧТЕНИЯ СПРАВОЧНИКОВ** приступайте к созданию/обновлению документации
- Используйте структуру и шаблоны из соответствующих справочников
- Следуйте YAML метаданным и XML тегам из шаблонов

<completion_criteria>
Документация создана/обновлена с использованием структуры из справочников
</completion_criteria>

<exception_handling>
Если структура не соответствует справочникам — пересоздайте документацию по шаблонам
</exception_handling>

### Step 7: Валидация качества

- Проверьте наличие YAML, TIER заголовков, XML тегов, системных якорей
- Убедитесь, что структура соответствует справочникам
- Пройдите MCP валидацию (цель ≥85)

<completion_criteria>
Документы соответствуют структуре справочников и проходят MCP проверку
</completion_criteria>

<exception_handling>
При недоборе баллов исправьте критические и предупреждения, затем повторите валидацию
</exception_handling>

</algorithm_steps>

<completion_criteria>
Все шаги выполнены, справочники прочитаны, терминология согласована, типы документов выбраны корректно, документация сгенерирована с использованием структуры из справочников и прошла MCP‑валидацию с баллом ≥85.
</completion_criteria>

<exception_handling>
Если любой из шагов не выполнен — остановите процесс и запросите выполнение пропущенного шага. Если справочники недоступны — запросите их чтение перед продолжением. Если MCP валидация не проходит — исправьте критические проблемы и повторите валидацию.
</exception_handling>

## 📚 TIER 3: Сопоставление терминологии

<terminology_mapping>

### Архитектурные концепции (combo-architecture.md)

| **Термин**                 | **Описание**                                                                                                                                                                                                                                                                        | **Область применения**                    |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| **Простой пакет**          | Одна модульная единица                                                                                                                                                                                                                                                              | `shared/lib/hooks/use-safe-back/`         |
| **Сложный пакет**          | Множество модульных единиц в FSD                                                                                                                                                                                                                                                    | `features/user/`, `shared/`               |
| **Модульная единица**      | Изолированный блок кода с публичным API через фасад (index.ts/index.tsx). Определяется как папка, содержащая фасадный файл, при этом на уровень выше этой папки нет другого фасадного файла. Все внешние части пакета могут импортировать из этой папки только через фасадный файл. | Любая папка с `index.ts` фасадом          |
| **Функциональный элемент** | Независимый компонент/хук/функция                                                                                                                                                                                                                                                   | `AuthForm`, `useAuth`, `validatePassword` |

### AI Documentation концепции

| **Справочник**                          | **Документирует**     | **Файл результата**  | **Соответствие архитектуре**          |
| --------------------------------------- | --------------------- | -------------------- | ------------------------------------- |
| **.cursor/docs/ai-module-template.md**  | «Модульные единицы»   | `module-ai-docs.md`  | ✅ Простые пакеты (модульные единицы) |
| **.cursor/docs/ai-package-template.md** | Пакеты с package.json | `package-ai-docs.md` | ✅ Сложные пакеты (workspace пакеты)  |

</terminology_mapping>

## 🧭 TIER 4: Правильное применение

<application_rules>

### Когда использовать module-ai-docs.md

✅ **Простые пакеты (модульные единицы):**

```text
shared/lib/hooks/use-safe-back/
├── index.ts                    # Основной хук + фасад
├── types.ts                    # Приватные типы
├── helpers.ts                  # Вспомогательные функции
└── module-ai-docs.md         # 🎯 AI документация модуля
```

✅ **Отдельные слайсы в сложных пакетах:**

```text
features/user/auth/
├── ui/auth-form/index.tsx
├── model/store/index.ts
├── service/auth-api/index.ts
├── index.ts                    # Фасад слайса
└── module-ai-docs.md         # 🎯 AI документация слайса
```

### Когда использовать package-ai-docs.md

✅ **Сложные пакеты (workspace уровень):**

```text
executables/mcp-validator/
├── src/
│   ├── services/
│   └── lib/
├── package.json               # Признак пакета
└── package-ai-docs.md              # 🎯 AI документация пакета
```

✅ **FSD слои как пакеты:**

```text
shared/
├── api/
├── ui/
├── lib/
├── model/
└── package-ai-docs.md              # 🎯 AI документация всего shared
```

</application_rules>

## 🧩 TIER 5: Ссылки на справочники

<references>
- **КРИТИЧЕСКИ ВАЖНО - ЧИТАТЬ ПЕРЕД ГЕНЕРАЦИЕЙ:**
  - `.cursor/docs/ai-package-template.md` - шаблон пакетной документации
  - `.cursor/docs/ai-module-template.md` - шаблон модульной документации
- **ДОПОЛНИТЕЛЬНО:** Изучите архитектурные принципы: `.cursor/docs/architecture.md`
- **ГЕНЕРАЦИЯ XML АРХИТЕКТУРЫ:** Для создания/обновления `architecture.xml` файлов, на которые ссылается `package-ai-docs.md`, используйте промпт `.cursor/docs/generate-architecture-xml.md`.

**⚠️ ПРЕДУПРЕЖДЕНИЕ: Генерация документации БЕЗ чтения справочников ЗАПРЕЩЕНА!**
</references>

## 🪢 TIER 6: Конфликты терминологии и решение

<conflicts_and_resolution>

### Термин «модуль» используется по-разному

— В модульной AI документации «модульная единица» соответствует архитектурной модульной единице
— В пакетной AI документации «модуль» = архитектурная группировка внутри пакета (например: `services`, `lib`, `model`)

### Решение конфликта

— В AI документации модулей используйте термин «модульная единица»
— В AI документации пакетов используйте «архитектурный модуль»

</conflicts_and_resolution>

## 🧮 TIER 7: Алгоритм выбора и генерации документации

<selection_algorithm>

1. Есть `package.json`? → `package-ai-docs.md` (пакетная документация)
2. Одна функция/компонент в папке? → `module-ai-docs.md` (модульная документация)
3. Несколько связанных функций в папке с фасадом? → `module-ai-docs.md`
4. Крупный FSD слой (`shared`, `features`) → `package-ai-docs.md`
5. Если в `package-ai-docs.md` есть `module_docs`, создавайте `module-ai-docs.md` по указанным `type/rule/targets`

</selection_algorithm>

## 🧾 TIER 3: Output Format

<output_format>
This is a reference prompt for integrating AI docs with architecture. Expected model output consists of the following sections in Markdown, using XML tags as shown:

**🚨 ОБЯЗАТЕЛЬНЫЙ ПЕРВЫЙ ШАГ:**

- Прочитать `.cursor/docs/ai-package-template.md` и `.cursor/docs/ai-module-template.md`
- Изучить структуру, YAML метаданные и XML теги из справочников

**Затем сгенерировать:**

- `<terminology_mapping>` — согласованный словарь терминов
- `<application_rules>` — правила выбора типа документа
- `<module_docs_yaml>` — образцы YAML политики `module_docs`
- `<conflicts_and_resolution>` — типовые конфликты и решения
- `<selection_algorithm>` — пошаговый алгоритм выбора
- `<conclusion>` — краткие выводы

All responses must be in Russian.
</output_format>

## ✅ TIER 8: Заключение

<conclusion>
AI документация совместима с архитектурой при корректной настройке `module_docs` и соблюдении терминологии:

- `module-ai-docs.md` ↔ «модульные единицы» (простые пакеты, слайсы)
- `package-ai-docs.md` ↔ «сложные пакеты» (FSD структуры, workspace пакеты)

Терминология унифицирована: для модульной документации — «модульная единица», для пакетной — «архитектурный модуль/пакет».
</conclusion>

## 📏 TIER 9: Completion Criteria

<completion_criteria>

- Терминология согласована и зафиксирована в `<terminology_mapping>`
- Для всех кейсов выбран корректный тип документа (`module-ai-docs.md` или `package-ai-docs.md`)
- Политика `module_docs` описана и применима к проекту
- Разрешены терминологические конфликты с явными пояснениями
- Документ проходит MCP валидацию с баллом ≥85

</completion_criteria>

[REFERENCE-END]
